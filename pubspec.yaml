name: betchecker_system_bars
description: Android system bar configuration for Betchecker.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.betchecker.systembars
        pluginClass: BetcheckerSystemBarsPlugin
